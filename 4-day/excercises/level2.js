//Escriba un código que pueda calificar a los estudiantes de acuerdo con sus puntajes:
/* const calif  = parseInt(prompt('Ingrese la calificacion del alumno'))

const result  = (calif) => {
    switch (true){
        case calif >= 80:
            alert('La calificacion del alumno es A')
            break;
        case calif >= 70:
            alert('La calificacion es B')
            break;
        case calif >= 60:
            alert('La calificacion del alumno es C')
            break;
        case calif >= 50:
            alert('La calificacion del alumno es D')
            break;
        default:
            alert('La calificacion del alumno es F')
            break;

    }
}

result(calif) */

//Consulta si la temporada es Otoño, Invierno, Primavera o Verano. Si la entrada del usuario es :
/* const mes = prompt("Ingrese el mes en el que esta");

const temp = (mes) => {
    switch(true){
        case mes === 'Septiembre' || mes === 'Octubre' || mes === 'Noviembre':
            alert(`La temporada en ${mes} es Otoño`)
            break;
        case mes === 'Diciembre' || mes === 'Enero' || mes === 'Febrero':
            alert(`La temporada en ${mes} es Invierno`)
            break;
        case mes === 'Marzo' || mes === 'Abril' || mes === 'Mayo':
            alert(`La temporada en ${mes} es Primavera`)
            break;
        case mes === 'Junio' || mes === 'Julio' || mes === 'Agosto':
            alert(`La temporada en ${mes} es Verano`)
            break;
    }
        
}

temp(mes); */

//Compruebe si un día es un día de fin de semana o un día laborable. Su script tomará el día como entrada.
/* const dia = prompt('Ingrese el dia en que se encuentra');
const oracion = (dia) => {
    if(dia.toLowerCase() === 'sabado' || dia.toLowerCase() === 'domingo'){
        alert(`El ${dia} es fin de semana`)
    }else{
        alert(`El ${dia} es un dia laborable`)
    }
}

oracion(dia) */